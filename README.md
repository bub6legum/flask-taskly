# Flask Taskly

A clean and simple Todo List web app built with **Python + Flask + SQLite**.

### Features
- Add, complete, and delete tasks
- Persistent storage with SQLite
- Flash messages for feedback
- Clean, responsive design

### How to run

1. Clone the repo:
   ```bash
   git clone https://github.com/yourusername/flask-taskly.git
   cd flask-taskly
